include ../button/button
//-
  +upload({
    accept: '.jpg, .png',
    maxSize: '10' // МБ
  })

mixin upload(data)
  .upload
    input(id='upload-input' type='file' required='true' data-accept=data.accept data-max-size=data.maxSize name=data.name multiple=data.multiple || false).upload__input.js-upload-input
    +button({
      tag: 'button',
      mods: 'js-upload-button',
      text: 'Выбрать файл'
    })